# CeylonSwift's canonical server environment template is:
#   backend/.env.example
#
# Create the local backend environment from the repository root with:
#   Copy-Item backend/.env.example backend/.env
#
# Do not put credentials in this root file. The browser runtime configuration is
# public by design and lives in js/config/public-runtime-config.js.
